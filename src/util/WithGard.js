
/*import { useSelector } from 'react-redux';

import { Navigate } from "react-router-dom";


const WethGard = (Component) => {
  const Conect = (props) => {

      const { user } = useSelector((state)=> state.authReducer.authData);
     
      return user? (
      <Component { ...props } /> 
      ): (
      <Navigate to="auth"/>
      );
  };
  return Conect;
}

export default WethGard*/